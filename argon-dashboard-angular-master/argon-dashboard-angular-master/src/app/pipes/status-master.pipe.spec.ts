import { StatusMasterPipe } from './status-master.pipe';

describe('StatusMasterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusMasterPipe();
    expect(pipe).toBeTruthy();
  });
});
