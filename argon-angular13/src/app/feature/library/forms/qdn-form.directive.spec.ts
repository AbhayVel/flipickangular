import { QdnFormDirective } from './qdn-form.directive';

describe('QdnFormDirective', () => {
  it('should create an instance', () => {
    const directive = new QdnFormDirective();
    expect(directive).toBeTruthy();
  });
});
