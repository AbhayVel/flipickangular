import { QdnModelDirective } from './qdn-model.directive';

describe('QdnModelDirective', () => {
  it('should create an instance', () => {
    const directive = new QdnModelDirective();
    expect(directive).toBeTruthy();
  });
});
